# Conda build variant configuration for UMCP
# Controls which Python versions and platforms to build for.

python:
  - "3.11"
  - "3.12"

# NumPy pins follow conda-forge conventions
numpy:
  - "1.24"
  - "1.26"

# Platform-specific overrides
# (noarch: python means one build covers all platforms)

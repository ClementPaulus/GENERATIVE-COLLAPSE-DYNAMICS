"""
GCD Domain Closures — Generative Collapse Dynamics

Tier-2 expansion providing core GCD-specific closure modules that extend
the kernel with energy potential, entropic collapse, field resonance,
momentum flux, generative flux, and universal regime calibration.

Modules:
    energy_potential.py            — Energy landscape and potential wells
    entropic_collapse.py           — Entropy-driven collapse dynamics
    field_resonance.py             — Field resonance patterns
    generative_flux.py             — Generative flux computation
    kernel_structural_theorems.py  — 7 structural theorems (T-KS-1 through T-KS-7)
    momentum_flux.py               — Momentum flux and conservation
    reflexive_closure.py           — 5 reflexive theorems (T-RF-1 through T-RF-5)
    universal_regime_calibration.py — Cross-domain regime calibration

Axiom-0 derivation:
    "Collapse is generative; only what returns is real."
    → The GCD closure is the self-referential domain: the kernel analyzing
      its own structural properties through the same invariants it defines.
    → Structural theorems (T-KS) prove properties of the kernel function
      using the kernel function itself — closure in the mathematical sense.
"""

from __future__ import annotations

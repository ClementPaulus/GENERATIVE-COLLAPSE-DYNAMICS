Tighten active files: centralize constants, vectorize kernel, eliminate duplication

- frozen_contract.py: Add _bisect() helper (pure-numpy, no scipy), compute
  C_STAR (0.7822), OMEGA_TRAP (0.6823), C_TRAP (0.3177) at module load
- kernel_optimized.py: Vectorize _compute_entropy() — replace Python loop
  with np.where + np.dot one-liner
- deep_diagnostic.py: Import gamma_omega/C_STAR/C_TRAP/OMEGA_TRAP from
  frozen_contract instead of local reimplementations
- cross_domain_bridge.py: Use C_STAR/C_TRAP/OMEGA_TRAP from frozen_contract
  instead of recomputing via brentq at module level
- cross_domain_bridge_phase2.py: Same constant centralization; retain brentq
  for parametric p-sweep only

All 5,247 tests pass. Lint clean. Integrity updated.
